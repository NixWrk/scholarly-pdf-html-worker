services:
  zotero-pdf-html-worker:
    build: .
    image: zotero-pdf-html-worker:local
    volumes:
      - ./output:/output
      # Mount the folder that contains source PDFs when running conversion.
      # - D:/work/pdfs:/pdfs:ro
    command: ["pdf-html-polish", "--help"]
